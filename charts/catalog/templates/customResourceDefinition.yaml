{{- if .Values.customResourceDefinition.enabled }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: modelorchestras.models.titanml.co
spec:
  group: models.titanml.co
  names:
    kind: ModelOrchestra
    listKind: ModelOrchestraList
    plural: modelorchestras
    singular: modelorchestra
  scope: Namespaced
  versions:
    {{- toYaml .Values.customResourceDefinition.versions | nindent 4 }}
{{- end }}
